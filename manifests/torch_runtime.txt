# Shared PyTorch runtime — CPU build.
# Installed once on demand by the first AI tool the user activates
# (vocal_isolator, upscaler, ocr_easy, whisper). Subsequent tools see
# this dir on sys.path and skip torch entirely (--no-deps in registry).
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1
numpy==1.26.4
