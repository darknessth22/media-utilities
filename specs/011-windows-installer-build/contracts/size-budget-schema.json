{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SizeBudget",
  "description": "Committed build size thresholds. Auto-generated on first build; enforced thereafter.",
  "type": "object",
  "required": ["installer_mb", "installed_mb", "tolerance_pct", "generated_at"],
  "properties": {
    "installer_mb": {
      "type": "number",
      "minimum": 1,
      "description": "Max installer .exe size in MB"
    },
    "installed_mb": {
      "type": "number",
      "minimum": 1,
      "description": "Max total unpacked install directory size in MB"
    },
    "tolerance_pct": {
      "type": "integer",
      "minimum": 0,
      "maximum": 50,
      "description": "Allowed overage percentage before build fails"
    },
    "generated_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 timestamp when this budget was first seeded"
    },
    "note": {
      "type": "string",
      "description": "Human-readable instructions for maintainers"
    }
  },
  "additionalProperties": false
}
