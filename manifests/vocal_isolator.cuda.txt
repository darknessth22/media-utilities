# Vocal isolator (demucs) — runs on shared torch_runtime (CUDA variant).
# torch + torchaudio + numpy live in torch_runtime; --no-deps in registry.
demucs==4.0.1
einops==0.8.0
julius==0.2.7
lameenc==1.7.0
openunmix==1.3.0
pyyaml==6.0.2
tqdm==4.66.5
