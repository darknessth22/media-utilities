# Vocal isolator (demucs) — runs on shared torch_runtime (CUDA variant).
# torch + torchaudio + numpy live in torch_runtime; --no-deps in registry.
demucs==4.0.1
einops==0.8.0
julius==0.2.7
lameenc==1.7.0
openunmix==1.3.0
pyyaml==6.0.2
tqdm==4.66.5
# demucs imports dora (from dora.log import fatal) at separate-time; with
# --no-deps its chain must be pinned by hand like everything above.
dora-search==0.1.12
omegaconf==2.3.0
antlr4-python3-runtime==4.9.3
retrying==1.4.2
submitit==1.5.4
treetable==0.2.6
cloudpickle==3.1.2
# torchaudio needs the soundfile backend to save wav/flac stems (mp3 goes
# through lameenc instead); without it ta.save dies with "Couldn't find
# appropriate backend". soundfile bundles libsndfile in its wheel.
soundfile==0.13.1
cffi==2.0.0
pycparser==3.0
